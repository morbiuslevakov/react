import { Stack, styled } from '@mui/material'

export const Wrapper = styled(Stack)({
    width: '100vw',
    height: '100vh',
    background: "#1E1E1E",
    justifyContent: 'center',
    alignItems: 'center',
})